import type { GlossaryTerm } from '../../../types';

export const kafkaGlossary: GlossaryTerm[] = [
  {
    id: 'kafka-glossary-broker',
    term: 'Broker',
    simple: 'A Kafka server that stores messages and serves producers and consumers.',
    technical: 'A Kafka broker is a JVM process that manages a subset of topic-partition log files on disk, handles produce/fetch requests, and participates in the cluster metadata protocol (ZooKeeper or KRaft). One broker per cluster acts as the active controller.',
    keyWords: ['server', 'cluster node', 'partition leader', 'controller'],
    relatedTopics: ['kafka-broker-cluster', 'kafka-replication'],
  },
  {
    id: 'kafka-glossary-topic',
    term: 'Topic',
    simple: 'A named channel in Kafka where producers write and consumers read.',
    technical: 'A topic is a logical abstraction over one or more partitions. It is identified by a name and has configuration properties such as replication.factor, retention.ms, and cleanup.policy. Topics are the primary organisational unit for event streams.',
    keyWords: ['channel', 'stream name', 'partition container', 'retention'],
    relatedTopics: ['kafka-topic-partition', 'kafka-overview'],
  },
  {
    id: 'kafka-glossary-partition',
    term: 'Partition',
    simple: 'A numbered subdivision of a topic — the unit of ordering, parallelism, and replication.',
    technical: 'A partition is an ordered, immutable sequence of records stored as log segment files on a broker. Each record has a unique offset within the partition. Ordering is guaranteed within a partition. Partitions are the unit of consumer parallelism and replication.',
    keyWords: ['ordering unit', 'log segment', 'offset sequence', 'parallelism'],
    relatedTopics: ['kafka-topic-partition', 'kafka-consumer-group'],
  },
  {
    id: 'kafka-glossary-offset',
    term: 'Offset',
    simple: 'A position number for a record within a partition — like a page number in a book.',
    technical: 'A 64-bit monotonically increasing integer that uniquely identifies a record\'s position within a partition. Consumer groups track their progress by committing their current offset to the internal __consumer_offsets topic.',
    keyWords: ['position', 'sequence number', '__consumer_offsets', 'commit', 'replay'],
    relatedTopics: ['kafka-offset', 'kafka-consumer'],
  },
  {
    id: 'kafka-glossary-consumer-group',
    term: 'Consumer Group',
    simple: 'A named team of consumers that shares the work of reading a topic — each partition goes to exactly one member.',
    technical: 'A set of consumer instances sharing a group.id. The group coordinator broker assigns partitions to members so each partition is consumed by exactly one member at a time. Multiple groups read the same topic independently with their own offset tracking.',
    keyWords: ['group.id', 'partition assignment', 'independent reads', 'group coordinator'],
    relatedTopics: ['kafka-consumer-group', 'kafka-offset'],
  },
  {
    id: 'kafka-glossary-isr',
    term: 'ISR (In-Sync Replica)',
    simple: 'The set of partition replicas that are sufficiently caught-up with the leader to be eligible for leadership.',
    technical: 'A follower replica stays in the ISR if it fetches from the leader within replica.lag.time.max.ms. The ISR shrinks when a follower falls behind. With acks=all, the producer waits for all ISR members to acknowledge. min.insync.replicas defines the minimum ISR size required to accept writes.',
    keyWords: ['replica health', 'acks=all', 'min.insync.replicas', 'leader election eligibility', 'replica lag'],
    relatedTopics: ['kafka-replication', 'kafka-producer'],
  },
  {
    id: 'kafka-glossary-consumer-lag',
    term: 'Consumer Lag',
    simple: 'How many records a consumer group is behind the latest message in a partition.',
    technical: 'Consumer lag = Log End Offset (LEO) − consumer group committed offset, calculated per partition. A growing lag indicates the consumer is slower than the producer rate. Monitored via kafka-consumer-groups.sh, JMX, or Prometheus exporters.',
    keyWords: ['LEO', 'committed offset', 'processing backlog', 'monitoring signal'],
    relatedTopics: ['kafka-monitoring', 'kafka-offset'],
  },
  {
    id: 'kafka-glossary-schema-registry',
    term: 'Schema Registry',
    simple: 'A central store for Avro/Protobuf/JSON schemas that enforces compatibility rules so producers and consumers always agree on message structure.',
    technical: 'A Confluent (or open-source Apicurio) service that stores schemas with numeric IDs. Producers register schemas at startup; the 4-byte schema ID is embedded in each Kafka record alongside a magic byte. Consumers fetch schemas by ID for deserialization. Enforces BACKWARD, FORWARD, or FULL compatibility at registration time.',
    keyWords: ['schema ID', 'magic byte', 'compatibility', 'BACKWARD', 'FORWARD', 'Avro'],
    relatedTopics: ['kafka-avro-schema-registry'],
  },
  {
    id: 'kafka-glossary-dlq',
    term: 'DLQ (Dead Letter Queue)',
    simple: 'A Kafka topic where records that fail processing after all retries are routed, rather than being lost or blocking progress.',
    technical: 'A DLQ is a designated Kafka topic (conventionally named originalTopic.DLT) that receives unprocessable records alongside an error envelope (source topic, partition, offset, exception class, stack trace). Prevents poison-pill records from blocking partition progress. Records are retained for inspection and selective replay.',
    keyWords: ['poison pill', 'error handling', 'retry exhaustion', 'replay', 'error envelope'],
    relatedTopics: ['kafka-dead-letter', 'kafka-consumer'],
  },
  {
    id: 'kafka-glossary-rebalance',
    term: 'Rebalance',
    simple: 'The protocol that redistributes partition ownership among consumer group members when the group composition changes.',
    technical: 'Triggered by consumers joining/leaving, partition count changes, or heartbeat timeouts. Eager rebalancing pauses all consumers and reassigns all partitions. Cooperative-sticky rebalancing (recommended) moves only changed partitions, allowing others to continue. Implemented via JoinGroup and SyncGroup requests to the group coordinator.',
    keyWords: ['partition redistribution', 'eager', 'cooperative-sticky', 'JoinGroup', 'SyncGroup', 'processing pause'],
    relatedTopics: ['kafka-rebalancing', 'kafka-consumer-group'],
  },
  {
    id: 'kafka-glossary-kraft',
    term: 'KRaft',
    simple: 'Kafka\'s built-in Raft-based metadata mode that replaces ZooKeeper — simpler to operate and scales to millions of partitions.',
    technical: 'KRaft (Kafka Raft) uses a subset of brokers as a Raft quorum to manage cluster metadata (topics, partitions, ISR, configurations) without an external ZooKeeper ensemble. Default since Kafka 3.3. Faster leader elections, no ZooKeeper operational burden, and supports significantly more partitions per cluster.',
    keyWords: ['Raft consensus', 'no ZooKeeper', 'cluster metadata', 'controller quorum', 'scalability'],
    relatedTopics: ['kafka-broker-cluster'],
  },
  {
    id: 'kafka-glossary-kafka-streams',
    term: 'Kafka Streams',
    simple: 'A Java library for building stateful stream-processing applications that run as regular JVM services without a separate cluster.',
    technical: 'A client-side library that defines processing topologies (DAGs of transform/filter/aggregate steps) using a DSL or Processor API. State is stored in local RocksDB stores backed by Kafka changelog topics. Supports exactly-once semantics (processing.guarantee=exactly_once_v2) and runs as a standard Java application.',
    keyWords: ['KStream', 'KTable', 'state store', 'topology', 'RocksDB', 'no separate cluster'],
    relatedTopics: ['kafka-streams', 'kafka-exactly-once'],
  },
];
